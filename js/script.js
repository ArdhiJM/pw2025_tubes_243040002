// Custom JavaScript for Muara Rahong Travel (optional)
// You can add interactive elements here later, e.g.,
// - Smooth scrolling
// - Image carousels (if not using Bootstrap's default)
// - Form validation (client-side)

document.addEventListener("DOMContentLoaded", function () {
  console.log("Muara Rahong Travel Frontend Loaded!");

  // Example: Add a subtle fade-in effect to sections on scroll
  // (Requires Intersection Observer API, more advanced)
  // const sections = document.querySelectorAll('section');
  // const observer = new IntersectionObserver(entries => {
  //     entries.forEach(entry => {
  //         if (entry.isIntersecting) {
  //             entry.target.classList.add('fade-in-visible');
  //         }
  //     });
  // }, { threshold: 0.1 }); // Trigger when 10% of element is visible
  // sections.forEach(section => {
  //     section.classList.add('fade-in'); // Add initial hidden state in CSS
  //     observer.observe(section);
  // });
});
